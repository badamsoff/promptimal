class GeminiService {
  // Placeholder for future integration
  Future<String> generatePrompt(String instruction) async {
    await Future.delayed(const Duration(milliseconds: 300));
    return 'Generated prompt for: $instruction';
  }
}
